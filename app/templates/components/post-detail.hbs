<div class="row">
	<div class="col-md-6">
		<h2>{{post.title}}</h2>
		<p>{{post.body}}</p>
		<p>{{post.teaser}}</p>
		<p>Author:<br>{{post.owner}}</p>
	</div>
	<div class="col-md-6">
		<div class="">
			<img src={{post.image}} style="height:330px; width: 47	0px"/>
		</div>
	<!-- delete button -->
	<button class="btn btn-danger"{{action 'delete' post}}>Delete Post</button>
	</div>
</div>
